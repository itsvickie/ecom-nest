export interface EnvironmentInterface {
  NODE_ENV: 'test' | 'development';
  APP_PORT: string;
  DB_URL: string;
  DB_HOST: string;
  DB_PORT: string;
  DB_USERNAME: string;
  DB_PASSWORD: string;
  DB_NAME: string;
}
